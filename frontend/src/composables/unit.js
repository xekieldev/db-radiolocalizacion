export function useUnit() {
    const unidad = [{
        value: 'KHz',
        label: 'KHz'
    }, {
        value: 'MHz',
        label: 'MHz'
    }, {
        value: 'GHz',
        label: 'GHz'
    }]

    return { unidad }
}